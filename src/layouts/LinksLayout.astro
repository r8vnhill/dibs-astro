---
import NotesSection from "./NotesSection.astro";

export interface Props {
  title?: string;
  id?: string;
  class?: string;
}

const {
  title = "En otros lenguajes…",
  id = "lang-links",
  class: className = "",
}: Props = Astro.props;
---

<NotesSection
  id={id}
  class:list={["mt-8", className]}
  aria-labelledby={`${id}-title`}
>
  <h3 id={`${id}-title`} class="text-lg font-semibold text-primary">
    {title}
  </h3>

  <div
    class="mt-3 grid grid-cols-1 gap-2 sm:grid-cols-2 md:grid-cols-3"
    role="list"
  >
    <slot />
  </div>
</NotesSection>
