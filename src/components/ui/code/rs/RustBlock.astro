---
import * as icons from "@ravenhill/astro-icons";
import CodeBlock from "../CodeBlock.astro";

type Props = {
    code: string;
    title?: string;
};

const { code, title }: Props = Astro.props;
---

<CodeBlock
    lang="rust" icon={icons.FileRs}
    {code} {title}
>
    <Fragment slot="title">
        <slot name="title" />
    </Fragment>
    <Fragment slot="source">
        <slot name="source" />
    </Fragment>
</CodeBlock>
