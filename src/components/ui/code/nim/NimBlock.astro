---
import * as icons from "$icons";
import CodeBlock from "../CodeBlock.astro";

type Props = {
    code: string;
    title?: string;
};

const { code, title }: Props = Astro.props;
---

<CodeBlock
    lang="nim" icon={icons.Code}
    {code} {title}
>
    <Fragment slot="title">
        <slot name="title" />
    </Fragment>
    <Fragment slot="source">
        <slot name="source" />
    </Fragment>
    <Fragment slot="footer">
        <slot name="footer" />
    </Fragment>
</CodeBlock>
