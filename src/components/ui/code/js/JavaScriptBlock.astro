---
/**
 * JavaScriptBlock.astro
 * ---------------------
 * Specialized code block for JavaScript snippets used in comparative lessons and
 * small CLI examples.
 */
import * as Icons from "@ravenhill/astro-icons";
import { CodeBlock } from "~/components/ui/code";

const { code, title, class: className = "" } = Astro.props;
---

<CodeBlock
    lang="javascript" icon={Icons.FileJs}
    {code} {title}
    class={className}
>
    <Fragment slot="title">
        <slot name="title" />
    </Fragment>
    <Fragment slot="source">
        <slot name="source" />
    </Fragment>
    <Fragment slot="footer">
        <slot name="footer" />
    </Fragment>
</CodeBlock>
