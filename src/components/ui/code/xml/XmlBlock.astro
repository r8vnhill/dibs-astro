---
import * as icons from "$icons";
import CodeBlock from "../CodeBlock.astro";

type Props = {
    code: string;
    title?: string;
};

const { code, title }: Props = Astro.props;
---

<CodeBlock
    lang="xml" icon={icons.Xml}
    {code} {title}
>
    <Fragment slot="title">
        <slot name="title" />
    </Fragment>
    <Fragment slot="source">
        <slot name="source" />
    </Fragment>
</CodeBlock>
