---
/**
 * A themed, accessible callout/admonition block for "Abstract" sections.
 */
import CalloutShell from "./CalloutShell.astro";
import { calloutVariants } from "./shared";
import type { BaseCalloutProps } from "./shared";

const variant = "abstract" as const;
const variantConfig = calloutVariants[variant];

const { title = variantConfig.title, icon = variantConfig.icon, ...rest } =
  Astro.props as BaseCalloutProps & Record<string, any>;
const hasTitleSlot = Astro.slots.has("title");
---
<CalloutShell variant={variant} title={title} icon={icon} {...rest}>
  {hasTitleSlot && <slot name="title" slot="title" />}
  <slot />
</CalloutShell>
