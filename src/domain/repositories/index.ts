/**
 * Public repository barrel for domain navigation contracts.
 * Keep external imports pointed here so repository interfaces stay centralized.
 */
export type { LessonNavigationRepository } from "./LessonNavigationRepository";
