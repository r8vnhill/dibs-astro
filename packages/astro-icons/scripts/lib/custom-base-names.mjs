// Canonical custom-icon allowlist shared by inventory auditing, release-policy derivation, and
// export/publication planning. Single source of truth so the base-name list is never
// hand-duplicated across scripts.

export const CUSTOM_BASE_NAMES = Object.freeze([
    "bash",
    "csv",
    "json",
    "kotlin",
    "nushell-logo",
    "powershell",
    "python",
    "scala",
    "xml",
]);
